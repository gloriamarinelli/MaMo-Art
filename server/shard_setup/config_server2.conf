# shard_setup/config-server2.conf
sharding:
  clusterRole: "configsvr"
replication:
  replSetName: "configReplSet"  # Nome del replica set per il config server    
storage:
  dbPath: /data/configdb1  # Puoi modificare il percorso se necessario
net:
  bindIp: localhost
  port: 27020